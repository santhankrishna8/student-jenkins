pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                Git 
            }
        }

        stage('Build') {
            steps {
                bat 'mvn clean install'
            }
        }

        stage('Test') {
            steps {
                bat 'mvn test'
            }
        }
    }
}
